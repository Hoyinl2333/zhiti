# 测试报告

测试日期：2026-09-23。

## 自动检查

- 内容转换：5 项单元测试通过，覆盖答案标记清理、不安全 HTML、快速解法分段、确定性 ZIP 和路径穿越拒绝。
- 判断推理包：SQLite 完整性正常，7,703 题、30,812 个选项；SHA-256 为 `ddc3bdddc960e86d88c737b047b4e407a5de57782058228062154dca0433af22`。
- 资料分析包：SQLite 完整性正常，3,562 题、717 份材料、14,248 个选项；SHA-256 为 `2007a2aba6ca181115279930275f752cfeea10b07753b0e57324124c7cb32640`。
- 内容目录的 Ed25519 签名验证通过；相同输入重复构建所得 ZIP 字节一致。
- Go 服务测试全部通过，覆盖设备绑定、重复激活、未授权下载、Range、状态重载、令牌吊销和设备解绑。
- Android 4 项练习状态单元测试通过；`lintDebug`、发布版 R8 压缩和 `assembleRelease` 通过。
- APK 元数据确认包名 `com.xiaoyunduo.zhiti`、版本 `1.0.0`、最低 API 31、目标 API 37；APK v3 签名验证通过。

## 模拟器实测

API 37 窄屏模拟器完成：

- 私有 CA 激活、签名目录读取、两个全量题库包下载、SHA-256 校验和原子安装。
- 判断推理下载在 10% 强制停止，重开后从分片继续到 17%，最终完整安装。
- 资料分析下载过程中覆盖安装同签名新版，下载继续且已有记录保留。
- 飞行模式下抽取完整资料及 6 道关联小题；判断推理图片、四个选项和解析均从本地渲染。
- 验证提交前隐藏答案、答错记录、错题答对后订正、收藏与取消收藏、本组小结、进程重启恢复和旋转。
- 验证题图全屏缩放、科学推理 130 题入口、确认后清空记录并立即移除继续练习。

Android 12（API 31）模拟器完成：

- 最终签名 APK 冷启动正常；APK 内置的 Ed25519 提供者完成目录验签。
- 私有 CA 激活成功，题库目录可读，判断推理后台下载进入 2%，前台服务无崩溃。

服务端实测：

- `https://43.136.39.211/healthz` 经私有 CA 校验返回正常。
- 443 仅由 `zhiti-server` 使用；原有 80、8080、3306、8025 服务保持运行。
- 线上两份题库包摘要与本机构建产物一致；测试激活码均已吊销，交付码保持未使用。

## 尚需真机确认

当前环境没有可连接的实体 Android 手机，因此首次侧载和同签名真机覆盖升级需要在收件人的 Android 12 或更高版本手机上各执行一次。模拟器已覆盖相同的安装、升级、下载和本地记录迁移路径。
